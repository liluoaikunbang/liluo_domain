# OpenAI Skills Catalog

仅选取 `skill-creator` 的 `SKILL.md` 与该目录自己的 Apache-2.0 许可证，固定于 `49f948faa9258a0c61caceaf225e179651397431`。仓库内其他 Skill 可能采用不同许可证，不能继承本条结论。

用户候选中的 `skills/.curated/develop-web-game/` 在此次核验的 HEAD 中不存在，已记录为路径漂移，不保存虚假文件。
