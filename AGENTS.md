# 项目身份

这是“璃落的城堡”，一个长期生长的个人作品。

它的核心不是一次性工具，不是临时 Demo，也不是纯按钮堆叠的页面集合。

它的目标是：

**做一个像素风的冒险 RPG。**

主角、地图、事件、探索、场景氛围、少量系统玩法，都是为这个目标服务。

---

# 项目目的

这个项目首先是一个可以长期扩展的幻想世界。

重点是：

- 冒险感
- 探索感
- 事件驱动
- 角色与场景氛围
- 文字、立绘、地图共同表达内容

家具、布置、收集、功能页面，都只是世界的一部分，不应该反过来压过“冒险 RPG”本身。

不要把它做成纯 GalGame，也不要做成纯资料库。

---

# 技术栈

当前以以下技术栈为主：

- Vue3 + Vite
- Phaser（用于 2D 地图、移动、碰撞、事件）
- Pinia

如果某个功能用新的技术栈会明显更方便，可以先简短建议，但不要直接改用，必须先得到用户同意。

---

# 架构原则

- docs 存放项目文档
- 已有功能优先保持可用

## src/game 目录约定

- `core/`：放跨场景基础能力；目前主要是游戏创建、场景 key、事件/对话运行、玩家运行时状态与菜单数据解析。
- `scenes/`：只放 Phaser 场景本体；目前按 `BootScene -> MapLoadingScene -> WorldScene` 串主流程。
- `systems/`：放可复用运行逻辑，不直接承担场景生命周期；当前主要分为 `map/`、`character/`、`animation/`、`environment/`。
- `systems/map/`：地图加载、出生点、碰撞、寻路、镜头、运行时整理、地图渲染等地图核心逻辑。
- `systems/character/`：玩家移动、输入、路径预览等角色控制相关逻辑。
- `systems/animation/`：角色与世界动画的注册、资源准备、复用逻辑。
- `systems/environment/`：昼夜、天气、雾层等地图环境表现逻辑。
- `data/`：放游戏数据、资源清单、配置与注册表；全局菜单静态数据放 `data/global/`，地图由 `data/registry.ts` 汇总，互动小说由 `data/interactive_fictions/registry.ts` 汇总。
- `data/maps/`：放地图内容数据；单张地图使用 `data/maps/<mapId>/`。
- `data/interactive_fictions/`：放互动小说/副本数据；单个副本使用 `data/interactive_fictions/<scenarioId>/`。
- `data/globalAssets.ts`、`data/assets.ts`：全局共享资源入口与资源 bundle 工具；角色、通用图片等优先放这里。
- `data/playerCharacter.ts`：主角显示与默认角色配置；角色基础设定优先集中维护。
- `data/animations.ts`：全局动画定义类型或公共动画配置；地图专属动画放到 `data/maps/<mapId>/animations.ts`。
- `data/maps/<mapId>/`：单张地图自己的数据目录；当前常用 `map.json`、`events.json`、`dialogues.json`、`meta.ts`、`assets.ts`，需要地图专属动画时再加 `animations.ts`。
- `views/`：Vue 侧游戏挂载容器、模式 UI 与桥接逻辑；不放 Phaser 场景细节，也不直接沉淀菜单静态数据。
- `views/components/base/`：只放游戏视图自用的基础 UI 组件；菜单各栏用独立面板组件，`GameMenuOverlay` 只做菜单接入、状态桥接与焦点导航；`src/game` 内的 UI 不再从外部 `src/components` 引用。
- 新增地图内容时，优先补齐 `data/maps/<mapId>/` 并接入 `data/registry.ts`，不要并行再造一套入口。
- `BootScene` 只处理启动期全局资源；地图资源加载优先交给 `MapLoadingScene` 按 `mapId` 动态处理。
- `WorldScene` 只负责场内组织；具体地图运行逻辑尽量下沉到 `systems/`，原始数据尽量留在 `data/`。
- 目录职责拿不准时，优先保证后续地图、事件、剧情能继续扩展，不为省几行代码打乱分层。

新增功能时，优先考虑：

- 是否方便继续扩展地图与事件
- 是否方便继续扩展剧情与状态
- 是否会破坏原有内容

---

# 风格原则

- 像素风优先
- 朴素、稳定、可维护
- 命名清晰
- 不为了炫技复杂化
- 可扩展，但不要过度设计

允许先粗糙可玩，再慢慢变完整。

不要为了追求一次到位，而阻塞功能落地。

生成或整理角色像素行走图时：

- 必须优先使用 `docs/系统说明/像素行走图生成提示词模板.md`。
- 默认要求同时提供 Image A（精灵表版式硬模板）和 Image B（角色外观参考）；不得把 Image A 的角色外观带入成品。
- 默认输出 `5 列 × 4 行`、四方向、每方向 idle 加四帧行走、纯绿色 `#00FF00` 背景的完整精灵表。
- 角色的发型、发色、五官、肤色、服装、配饰和鞋袜等定制特征一律以 Image B 为准，不在通用提示词中写死。
- 正式接入前必须人工核对 20 帧、方向顺序、真实转身、脚底基线、相对留白和色板稳定性；不要用运行时代码补偿生成图的错位。

---

# 开发原则

- 先保证可玩，再补细节
- 先保证主流程，再补边角功能
- 地图、事件、对话、存档是核心
- UI、说明页、资料页是辅助
- docs中的文档统一 UTF-8 保存
- 读写文件中的中文时，一定要注意 UTF-8 编码，防止中文变成乱码。

不要把问题藏起来。

不要用假数据、内置伪结果、绕路输出去掩盖真实问题。
如果确实缺资源、缺素材、缺外部条件，要直接提醒用户。
改动时，尽量不要改动各个地图数据的map.js，因为这里的数据是可能随时更新的，新增的内容都可能在之后失效。

调整故事大纲、任务树等嵌套 JSON 时：

- 新增故事大纲条目必须使用 `docs/系统说明/故事大纲条目模板.md`；Markdown YAML 统一包含模板列出的 13 个字段，暂无内容的字段留空，不用假数据占位。
- 大纲 Markdown 文件名中的标题必须与来源 JSON 节点的 `title` 一致，并保存到该节点所属世界的 `src/game/data/story_outline/<世界目录>/` 下。
- 默认只移动用户明确指定的节点，不连带移动其子节点；如果需要连带移动，必须由用户明确说明。
- 节点移动前后要核对 key 数量、重复 key 和关键节点是否仍存在；除非用户明确要求删除，否则不允许让节点消失。
- 复杂节点移动优先用按 key 操作的小脚本完成，不手动剪贴大段 JSON。
- side 分支只表示显示布局，不代表整条后续主链都要变成 side。
- 除非用户明确说明新条目属于哪个节点的 side，否则不得新增 `branchLayout: "side"`，也不得把新条目接成已有节点的并列 child。
- 用户要求在节点 A 下方加入节点 B，且原链为 `A → C` 时，默认改为 `A → B → C`：令 `B.parentKey = A.key`，并把原直接后继 `C.parentKey` 改为 `B.key`，保证单向连接。
- 如果目标节点已经存在多个直接子节点，而用户没有说明要插入哪条链或属于哪个 side，不得自行猜测，必须先询问用户。

不要随意简化用户方案。
不要擅自改成别的路线。
如果要偏离用户方案，必须先征得同意。

---

# 内容原则

世界观、角色、地点、事件，应该逐步沉淀成可复用内容。

新内容优先服务以下核心体验：

- 在地图中移动
- 发现新地点
- 触发事件
- 推进剧情
- 形成记忆点

装修、布置、收集等内容，优先做成地图世界中的互动，而不是独立的专业工具。

---

# Documentation rule

当某个功能真正实现完成，并且经过用户实际测试确认无报错后，必须同步更新项目文档，之后该任务才算真正完成。

文档至少需要简洁但完整地记录：

- 本次功能的实现思路
- 相关代码路径
- 开发过程中遇到的问题
- 对应问题的解决方法

未完成文档记录前，不应将该功能视为完全完成。

当用户实际测试确认无误并完成上述文档记录后，应同步将本次相关改动提交并上传 GitHub；上传时只包含本次功能范围内的文件，不得夹带工作区中的其他本地改动。

提交并上传 GitHub 时，默认直接在 `main` 分支提交并推送，不主动创建功能分支、临时分支或 PR。只有用户明确要求使用分支、隔离开发、代码审查或 PR 流程时，才创建新分支；如果当前改动已经位于其他分支，而用户没有要求保留多分支，应在确认可以安全快进或合并后同步到 `main`。

与 GitHub 双向同步时，不能只处理新增和修改项：本次功能范围内已删除的文件或目录也必须在提交中同步删除；从 GitHub 下载或更新本地时，也必须落实远端已有的删除，避免失效内容在本地或远端持续残留。同步删除前仍需核对范围，不得误删用户未纳入本次任务的本地文件或改动。

---

# 注意事项

- 项目以 Windows 10 / Windows 11 为开发环境
- 不要使用 Linux 命令作为默认方案
- 尽可能自己完成修改，不要把本可定位的问题甩给用户
- 每次更新完成后，不要拉起新的页面自动打开，用户那边会自己开启服务进行验证的。
- 临时测试文件都放在scripts\tests中，且测试完成后如果后续不需要使用了就及时删除

每次新增 `src/assets/game` 下的素材图片后，都应及时同步更新素材清单，补充：

对`src`中的修改都尽量只改动 `src/game` 路径下的内容，其他路径的内容主要作为参考，如有改动要强调出来。

1. 素材文件路径
2. 素材文件名
3. 素材类型
4. 素材用途或适用场景

---

# 最重要的一句话

**这个项目首先应该越来越像一个像素风冒险 RPG，而不是越来越像一堆功能页面。**
