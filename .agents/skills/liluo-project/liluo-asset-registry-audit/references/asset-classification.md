# 素材分类

- 运行时成品：由 `src/game` 的资源 bundle/注册表直接加载。
- 制作源：PSD、母图、分层编辑文件或生成输入。
- 候选素材：尚未明确接入运行时的备选。
- 构建缓存：由 `scripts/assets/build-liluo-character-frames.ps1` 等流程可重建的帧。

分类必须来自路径、引用和文档证据；不凭扩展名自动升级。新增 `src/assets/game` 图片后，清单至少记录路径、文件名、类型与用途。
