# 图结构操作规则

来源是按世界拆分的扁平节点数组，由稳定 `key` 与 `parentKey` 建图。修改前后比较完整 key 集合。

- 单节点移动仅改目标 `parentKey`；子树移动只在用户明确要求时执行。
- 链中插入：B 指向 A，原直接后继 C 改指 B。
- side 仅控制显示布局。
- 重命名保持 key，并联动 JSON title、Markdown 文件名和正文标题。
- 拆分/合并先确认保留 key、废弃 key、引用迁移和内容归属。
