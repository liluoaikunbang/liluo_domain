# Model archive contract

`docs/写作资产/模型归档/model-lock.json` pins repository + revision metadata. Pin does not download weights. Weight download is a future explicit user-authorized operation outside Git.
