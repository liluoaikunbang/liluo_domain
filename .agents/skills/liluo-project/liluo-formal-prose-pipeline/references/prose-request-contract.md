# Prose request contract

Authority: `schemas/workflows/formal-prose-request.schema.json` and `docs/写作资产/模板/正式正文写作合同说明.md`.

Invalid contracts must fail before any remote call. `maxTokens` ≠ target Chinese character count. `styleReferenceIds` are explicit only.
