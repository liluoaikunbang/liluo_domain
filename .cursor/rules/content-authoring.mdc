---
description: 修改璃落正式内容数据、地图事件、叙事来源，或 RAG / Style-RAG 阅读向文案时的权威与审批边界。
globs:
  - src/game/data/**
  - external-knowledge/cards/**
  - docs/写作资产/**
  - docs/知识检索校准/**
alwaysApply: false
---

- 先按根 `AGENTS.md` 与 `project-navigation/context-policy.json` 的任务装载边界，读取匹配的 `.agents/skills/liluo-project/**/SKILL.md`，再修改正式内容。
- “情节、桥段、情节库”写入 `src/game/data/plot_outline/catalog.json`；“大纲、主线、节点”才进入故事大纲来源，除非用户明确要求关联两库。
- 候选灵感未经用户批准不得写入正式内容；结构性数据修改使用现有 schema、验证器和最小适用检查。
- 正式阅读向内容的成员复核按 `project-navigation/team-routing.json` 路由，不读取全员灵魂卡。
- 开放权重正式正文候选走 `liluo-formal-prose-pipeline`，只写入 `docs/写作资产/工作区/`；批准后由现有故事/对话 Skill 写入正式文件。
- 文风检索、外部文章评权与 Style Pack 走 `liluo-style-rag`（元数据 V0/V1）；不得用 embedding/向量库，不得把范例事实写入 canon。
- 紧缚 RAG 知识库走 `liluo-external-fiction-knowledge`；撰写或改写知识卡正文、Style-RAG 抽象说明、抽查协商与改稿说明前，先读并应用 `liluo-natural-expression` light；ID、路径、命令不润色。