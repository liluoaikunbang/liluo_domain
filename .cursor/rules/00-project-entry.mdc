---
description: 璃落项目的跨工具入口、权威来源与专属适配边界。
alwaysApply: true
---

- 先遵循仓库根目录 `AGENTS.md`；它是 Cursor 与 Codex 共用的最高层项目约束和工作流路由。
- 权威事实在普通项目文件中维护：`docs/系统说明/`、`.agents/skills/`、脚本、schema 与测试。项目索引只用于定位，修改前必须核验原始权威文件。
- `.cursor/` 只放 Cursor 规则和命令；不得复制或编辑 `.codex/` 的授权、Agent 或运行时配置来实现兼容。
- 需要为两个工具维护大段派生配置时，先向用户说明应采用一个普通源文件及生成/校验脚本；未经用户确认，不创建双端大段副本或文件系统链接。
