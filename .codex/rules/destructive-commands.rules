# match: ["Remove-Item", "target"] => prompt
# not_match: ["Get-ChildItem", "target"] => no project decision
prefix_rule(pattern = [["Remove-Item", "del", "rmdir", "rm"]], decision = "prompt", justification = "删除命令必须先核对目标、范围与可恢复性。")
