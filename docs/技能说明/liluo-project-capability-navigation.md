# 项目能力与行动导航

用于回答“我现在能做什么”和“下一步做什么”，并把 Skill、Agent、命令、故事缺口与玩法建设状态收束到可验证注册表。

权威数据不迁入导航：`project-navigation/` 只保留派生入口、证据与状态；两份 Markdown 均由脚本生成。相关变化运行 `npm run project:navigation:changed`，查询可用 `npm run project:navigation:next -- --time 20 --avoid map`。候选或计划不等于完成；玩法目录条目不等于已实现。
