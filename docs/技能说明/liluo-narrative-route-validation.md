# liluo-narrative-route-validation

对剧情路线执行 `static`、`simulate`、`save-resume` 与可选人工 `browser` 验证，检查缺失目标、不可达节点、死循环、条件、状态和结局。fixture 隔离，不修改真实存档。路径：`.agents/skills/testing/liluo-narrative-route-validation/`。
