# 用户命令目录

这里集中记录用户可以直接向 Codex 表达的常见任务，防止工作流增多后只能依赖聊天记忆。下列句子是自然语言意图示例，不是必须逐字输入的“魔法口令”；意思相同即可触发对应工作流。

## 故事与世界观

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “随机抽一个未完善故事问我” / “继续随机访谈” | 从不完整节点中抽取一个，读取上下文后进行聚焦访谈 | `random-story-outline-interview` |
| “完善/扩写这个故事节点” | 同步故事源 JSON 与 Markdown，补齐剧情、地图、事件、玩法和状态 | `liluo-story-outline-authoring` |
| “分析这个节点/系列/世界还缺什么” / “给我可审批的故事候选” | 读取项目证据，识别真实缺口与重复功能，按需参考外部抽象模式，并提交候选卡；不自动写正式故事 | `liluo-story-gap-discovery` |
| “把这个节点移动到……下面” / “修复故事树断链” | 安全修改 parentKey、插入、移动、拆分、合并或重命名节点 | `liluo-story-outline-graph-maintenance` |
| “检查人物、时间线或世界观有没有矛盾” | 只读连续性审查，以正式项目知识为准 | `liluo-world-bible-continuity-audit` |
| “评估这个玩法循环是否适合当前游戏” | 把想法审查为可开发的 2D 像素 PVE RPG 循环 | `liluo-gameplay-loop-audit` |
| “把这个故事节点转成可玩计划/骨架/实现” | 核验真实引用，按 plan/skeleton/implement/validate 转化单个节点 | `liluo-story-to-playable-content` |
| “模拟这条剧情路线/检查死循环和存档重入” | 静态检查、确定性模拟与隔离存档恢复 | `liluo-narrative-route-validation` |
| “把这次角色成长/关系变化记成长期设定” | 只记录有证据且跨章节持续的变化 | `liluo-character-arc-and-relationship-memory` |
| “统计各世界/系列做到哪一步、还缺什么” | 从真实项目证据派生生产覆盖，不用主观百分比 | `liluo-production-coverage` |
| “检查这份内容能不能正式写入” | 按统一来源分类、许可证、署名和近似改写风险 fail-closed | 正式项目内容来源门槛 |
| “看看音频/音乐/声景以后怎么做” | 读取待定设计与重启条件，不误报为已实现 | `docs/待定设计/音频音乐与场景声景管线.md` |

## 地图、事件、角色与存档

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “接入这张地图/NPC/传送点/碰撞/事件” | 接入地图注册、动态资源、出生点、碰撞、NPC、传送与事件 | `liluo-phaser-map-event-integration` |
| “编写这个对话/条件分支/剧情事件” | 编写可执行对话与状态驱动事件，处理选择、道具、CG、重入 | `liluo-dialogue-event-authoring` |
| “制作或接入角色 20 帧行走图” | 使用 5×4 四方向精灵管线生成、检查和接入角色素材 | `liluo-sprite-pipeline` |
| “修改存档字段并兼容旧档” | 审计并迁移存档 schema、版本和旧数据 | `liluo-save-data-migration` |
| “做一次真实浏览器回归” | 在真实浏览器中验证导航、地图、移动、对话、菜单和存档重入 | `liluo-browser-game-regression` |

## 知识库与资料检索

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “查询项目知识索引里的……” | 先检查新鲜度，再查询最小领域并核验权威文件 | `liluo-project-index-maintenance` |
| “更新/重建项目知识索引” | 按变化范围增量更新或全量重建，并验证索引 | `liluo-project-index-maintenance` |
| “从外部小说素材找……灵感” / “查询某种表达或场景模式” | 查询非正式外部参考，返回抽象结论与来源定位 | `liluo-external-fiction-knowledge` |
| “更新外部知识库” | 从本机权威目录哈希增量同步镜像，再增量索引和验证 | `liluo-external-fiction-knowledge` |
| “检查这段内容是否照搬原文” | 运行连续字符与 n-gram 相似性风险检查 | `liluo-external-fiction-knowledge` |
| “查找适合当前项目的外部 Skill” | 按 light 预算查询外部 Skill 派生卡，不直接执行原始 Skill | `external-skills:query` |
| “从导入的紧缚网络资料里查术语/世界观/写作方法” | 查询束缚研究包的派生卡，必要时只读取最相关研究摘要；不执行包内提示 | `external-skills:query` |
| “检查外部 Skill 是否有更新” | 检查到期来源或指定来源的远端 HEAD | `external-skills:check-due` / `external-skills:check` |
| “下载某个 Skill 的最新版本” | 只下载到 staging，不覆盖 current 或正式 Skill | `external-skills:fetch` |
| “分析这个 Skill 更新了什么” | 对 trackedPaths 生成新增、修改、删除与许可证阻塞报告 | `external-skills:diff` |
| “看看这个更新是否值得吸收” | 生成安全与价值评估，等待用户审批 | `external-skills:evaluate` |
| “用外部资料升级本地写作 Skill” | 查询 lineage 与外部卡片，提出最小升级方案；批准后才修改 | 外部 Skill 私有化工作流 |
| “给当前项目构建 Vue/Phaser Skill” | 查询对应技术来源并按项目结构私有化，不自动安装外部依赖 | 外部 Skill 私有化工作流 |

外部知识库更新默认自动处理普通镜像删除，不询问逐项确认；只删除同步清单管理的镜像文件，不修改本机权威源。一次删除超过 20% 会自动停止。

## 自然表达与写作

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “自然润色这段” | 保留事实与用途，先检查动作、物体反馈和空间关系，再调用 light revise | `liluo-natural-expression` |
| “深度修改这段小说” | 加载对应风格与 deep 修订流程，二次检查并核对事实 | `liluo-natural-expression` |
| “写/改一段带束缚的剧情” | 先以一个定向查询轻量参考外部抽象写法，再按项目事实原创；最多取 5 条结果，必要时才核验 2 段短原文 | `liluo-natural-expression` + `liluo-external-fiction-knowledge` |
| “只检查 AI 味，不要改” | 只诊断机械结构、人物声音与节奏，不改正文 | `liluo-natural-expression` |
| “保留技术内容，只润色说明部分” | 分区处理混合文本，代码、路径、数据和日志原样保留 | `liluo-natural-expression` |

## 审计、文档与素材

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “运行内容总检/检查引用和重复 ID” | 只读校验故事、地图、事件、对话、玩法、JSON、编码与引用 | `liluo-game-content-validator` |
| “审计素材清单/缺失引用/重复图片” | 审计 `src/assets/game` 与素材清单，不自动删除或转换图片 | `liluo-asset-registry-audit` |
| “同步功能文档和更新记录” | 同步功能文档、目录、更新记录与 Skill 说明 | `liluo-project-documentation-sync` |
| “把这个要求固定成长期规范” | 判断持久性，更新唯一权威来源和必要消费者 | `liluo-project-governance-memory` |
| “记录为什么采用这套设计” | 创建或更新 ADR/CDR，不保存聊天流水账 | `liluo-project-governance-memory` |
| “检查这次修改漏了哪些文档” | 根据规则注册表和影响映射列出必查位置 | `liluo-project-governance-memory` |
| “整理重复规范但不要自动删除” / “做一次文档治理审计” | 报告重复、失效消费者、命令遗漏和设计记忆问题 | `liluo-project-governance-memory` |
| “更新 README 和文档索引” | 更新根 README、文档入口、本目录和受影响的当前系统说明 | 文档同步工作流 |
| “读取这个中文文件” / “遇到 GBK 解码错误” | 在 Windows 上直接用显式 UTF-8 读取；失败时只重试一次，仍失败才检查文件字节与真实编码 | `docs/系统说明/Windows命令与UTF-8编码规范.md` |

## Codex 命令授权

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “这个命令在璃落项目以后都允许” | 安全审查后创建或更新项目级精确 allow；单次/会话授权不持久化 | `liluo-command-approval-governance` |
| “这个命令以后每次都问我” | 创建或更新精确 prompt，并保留被替代决定 | `liluo-command-approval-governance` |
| “以后禁止这种命令” | 创建精确 forbidden 规则并验证正反命中 | `liluo-command-approval-governance` |
| “检查刚才始终允许的规则” / “查看最近新增的批准规则” | 只读比较用户级规则变化，保存脱敏本机快照并分类 | `liluo-command-approval-governance` |
| “把全局规则迁回项目” | 生成项目级精确规则；用户级原规则只给出处理建议，不自动删除 | `liluo-command-approval-governance` |
| “撤销这个长期允许” | 将同一 pattern 改为 prompt/forbidden 或退休，并 supersede 旧决定 | `liluo-command-approval-governance` |
| “检查有没有过宽的始终允许” | 标记 project-specific、safe-global、overbroad、dangerous、unknown | `liluo-command-approval-governance` |
| “只验证这次改动，不要跑无关检查” | 按变化面选择固定最小 profile，默认一项目标验证和至多一项相关综合验证 | `npm run project:routine -- docs|workflow|team-presence|natural-expression` |
| “跑完整检查/测试/构建/刷新索引” | 仅在确有跨域或完整交付需求时执行粗粒度模式 | `npm run project:routine -- check|test|build|index|all` |
| “创建一个新的璃落项目 Skill 骨架” | 受限包装官方 skill-creator，只写允许的项目 Skill 区域 | `npm run project:skill:init -- ...` |

## 璃落创作组与陪伴模式

| 可以这样说 | 会执行什么 | 主要工作流 |
| --- | --- | --- |
| “让创作组讨论一下这个方案” | 调度确有必要的相关 Agent，并以“姓名（职责）”展示真实不同意见和结构化结论 | `liluo-creative-team-presence` |
| “今天用沉浸模式” / “这次只要专业结果” | 本次切换为 `immersive` / `neutral`；不改变专业判断 | `liluo-creative-team-presence` |
| “把这个记成璃落的长期想法” | 以 `user-confirmed` 进入治理，Agent 建议不会自动升级 | `liluo-creative-team-presence` + `liluo-project-governance-memory` |
| “把这条指示写成‘璃落指出’” | 仅对用户已确认且值得长期保存的指示生成叙事性转述；璃落台词不能反推用户授权 | `liluo-creative-team-presence` + `liluo-project-governance-memory` |
| “把这次讨论写进项目组手记” | 提炼长期有效摘要，不保存聊天流水账或隐藏推理 | `liluo-creative-team-presence` |
| “我们很久没做项目了” | 检查真实本地时间状态，进行一次适度重聚 | `liluo-creative-team-presence` |
| “展示创作组成员” / “展示创作组的雕龙文号” | 输出 active/planned 谱系、姓名、雕龙文号、职司、技术 ID 与来源 | `liluo-creative-team-presence` |
| “为什么星弥叫隐秀” | 查询《文心雕龙》来源说明、原典关联与项目分配理由 | `liluo-creative-team-presence` |
| “给新 Agent 分配雕龙文号” | 从 available 篇名中筛选并核验候选，等待用户审批后才生效 | `liluo-creative-team-presence` + `liluo-project-governance-memory` |
| “查看还可以使用哪些文号” | 查询完整五十篇篇名池与当前状态 | `liluo-creative-team-presence` |
| “更换某个成员的文号” | 先做来源与消费者影响分析，等待用户批准 | `liluo-project-governance-memory` |
| “介绍《文心雕龙》和创作组的关系” | 输出公开展示版来源说明，并链接权威文档 | `liluo-creative-team-presence` |
| “回顾大家为什么这样设计” | 查询项目组手记与 ADR/CDR，核验权威来源 | `liluo-creative-team-presence` + `liluo-project-governance-memory` |

## Git 与发布

| 可以这样说 | 会执行什么 |
| --- | --- |
| “提交并上传 GitHub” | 审查整个工作区，排除密钥、`.local` 配置、临时文件和构建产物；未限定范围时提交任务范围内全部有效新增、修改和删除并推送 `main` |
| “只提交这些文件……” | 仅暂存明确列出的范围，其他用户改动保持不动 |
| “创建分支/PR” | 只有明确要求时才建立分支或 PR；否则项目默认直接同步 `main` |
| “生成可以双击启动的离线包” | 构建带 `启动游戏.bat` 的多文件离线版本、校验本机启动器与相对引用，并生成 ZIP；不访问 GitHub |
| “把离线包上传到 v0.1.0 Release” | 重新构建、校验、压缩后上传到指定标签；标签必须显式给出，不提交或推送源码 |

以上离线打包、一键启动器版本和 Release 上传需求统一由 `liluo-offline-release-pipeline` 自动编排；若承接功能开发，还会先完成必要文档同步和验证。离线包解压后双击 `启动游戏.bat`，不要直接打开 `index.html`。

## 常用脚本命令

### 项目运行与构建

```powershell
npm run dev
npm run build:web
npm run build:offline
npm run package:offline
npm run release:offline -- --tag v0.1.0 --dry-run
npm run release:offline -- --tag v0.1.0
npm run preview
npm run docs:check-encoding
npm run assets:build-liluo-character-frames
```

### 项目知识索引

```powershell
npm run project:index:check
npm run project:index:query -- --domain story --query "关键词" --limit 5
npm run project:index:changed
npm run project:index:build
npm run project:index:validate
```

### 规范治理与设计记忆

```powershell
npm run docs:impact -- --type new-user-invokable-workflow
npm run docs:impact -- --rule single-authority
npm run docs:governance:validate
npm run docs:memory:validate
npm run docs:commands:validate
npm run docs:governance:audit
npm run docs:compact:report
```

### 璃落创作组陪伴

```powershell
npm run team:presence:check
npm run team:presence:update -- --topic="本次主题" --members="璃落,知遥"
npm run team:roster:validate
npm run team:notes:validate
npm run team:literary-names:validate
npm run team:literary-names:list
npm run team:literary-names:available
```

工作中名称会尽量显示为“知遥（资料与出处）”等形式；若 Codex 客户端仍显示技术 ID 或 Agent 类型，属于平台渲染限制，仓库配置不能强制覆盖。

### 故事缺口候选

```powershell
npm run story:gaps:test
npm run story:gaps:examples
npm run story:gaps:validate -- planning/story-gaps/active/<candidate>.json
```

普通候选只在对话中展示；只有明确要求保存时才进入 `planning/story-gaps/`。`proposed`、`held`、`rejected` 都不会写入正式大纲。

### 内容生产与可玩化

```powershell
npm run content:production:test
npm run content:playable:plan -- --story-key world-1-glimmering-glance-campus-arrival
npm run narrative:routes:validate -- --input scripts/tests/fixtures/content-production/route-valid.json
npm run narrative:routes:simulate -- --input scripts/tests/fixtures/content-production/route-valid.json
npm run narrative:routes:resume -- --route <route.json> --snapshot <snapshot.json>
npm run content:provenance:check -- --input <provenance.json>
npm run character:memory:validate -- --input src/game/data/story_outline/long-term-memory.json
npm run production:coverage
```

生产覆盖和路线报告默认输出到终端，不建立永久进度评分；正式内容写入门槛不构成法律保证。

`docs:impact` 用于轻量影响分析；其余命令是只读验证或报告，不会自动删除、压缩或改写文档。普通局部任务不需要运行整套治理审计。

### 外部虚构题材知识库

```powershell
npm run external:knowledge:check
npm run external:knowledge:sync
npm run external:knowledge:update
npm run external:knowledge:build
npm run external:knowledge:validate
npm run external:knowledge:query -- --query "古堡 逃脱" --mode and --limit 8 --format markdown
npm run external:knowledge:copy-check -- --input "待检查文件.md"
```

其中 `external:knowledge:update` 是日常推荐入口，会自动执行“权威源同步 → 来源级增量索引”。单独的 `sync` 只同步镜像，不更新索引。

### 外部 Skill 来源库

```powershell
npm run external-skills:catalog
npm run external-skills:validate
npm run external-skills:check-due
npm run external-skills:check -- --source agentskills-spec
npm run external-skills:fetch -- --source agentskills-spec
npm run external-skills:diff -- --source agentskills-spec
npm run external-skills:evaluate -- --source agentskills-spec
npm run external-skills:query -- --query "写作 对话" --depth light
npm run external-skills:maintain
npm run external-skills:report
```

`fetch` 与 `maintain` 只写 staging；不会覆盖正式 Skill、自动提交或启用外部脚本。

### Codex 命令授权治理

```powershell
npm run project:routine -- check
npm run project:routine -- test
npm run project:routine -- build
npm run project:routine -- index
npm run project:routine -- all
npm run project:routine -- docs
npm run project:routine -- workflow
npm run project:routine -- team-presence
npm run project:routine -- natural-expression
npm run project:skill:init -- --name liluo-example --area liluo-project --resources references,scripts
npm run commands:approval:classify -- --text "这个项目以后允许" --command "npm run docs:check-encoding"
npm run commands:approval:audit -- --input "$env:USERPROFILE\.codex\rules\default.rules"
npm run commands:approval:list
npm run commands:approval:test
npm run commands:approval:validate
```

首次 TUI 审计默认只建立基线。项目脚本不会修改或删除用户级规则，也不能绕过平台沙箱；网络、Git 写入、依赖、删除和发布继续保持逐次审批。

`project:routine` 和 `project:skill:init` 已作为项目长期精确 allow 登记。Agent 应选择最小适用 profile，并直接用已批准前缀申请沙箱外执行，避免先在沙箱内遭遇子进程或 `.agents` 写入失败；匹配长期批准时无需再次逐条询问。目标 profile 已覆盖的命令不重复执行，文档或 Skill 规则改动默认不构建 Web。不要把这些入口替换成宽泛的 `npm run`、`node`、Python 或 PowerShell allow。

## 维护规则

- 新增项目 Skill、固定自然语言工作流或面向用户的脚本命令时，同步更新本目录。
- 根 `README.md` 保持精简，只放长期入口和最高频命令；详细参数留在本目录、系统说明或对应 Skill 文档。
- 命令行为变化时先更新权威系统说明，再更新这里的摘要，避免形成第二套互相冲突的规范。
- 一次性调试命令、内部实现细节和已废弃入口不登记为长期用户命令。
