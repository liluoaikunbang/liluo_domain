# 璃落写作表

权威当前表：`current.json`。

- `status = awaiting-assets`：尚无足够用户批准的璃落文风证据。
- 证据优先级：黄金正文 > 修改对照 > 个人旧作 > 高评分外部文章 > 外部抽象风格卡。
- 外部文章不得单独定义璃落文风。
- 草稿在 `drafts/`，批准版本快照在 `versions/`，证据备忘在 `evidence/`。
- 批准命令：`npm run writing:style:sheet:approve -- --user-approved --draft <path>`
