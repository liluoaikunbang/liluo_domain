# 外部虚构题材知识库

本系统为璃落创作提供文学表达、画面结构、场景灵感、虚构状态与题材模式的外部参考，不是项目正式设定库。所有派生记录均为 `knowledgeScope: external-fiction-reference`、`canonical: false`。

## 来源与结构

用户只持续维护本机权威目录；当前机器由被 Git 忽略的 `external-knowledge/source-sync.local.json` 保存绝对路径。`external-knowledge/sources/fiction-bondage/` 是受控仓库镜像，构建器只索引镜像且从不反向修改权威源。Markdown/TXT 等纯文本进入分段索引，图片等不支持格式只编目。

- `sources/`：原始输入；`catalog/`：稳定来源 ID、哈希、元数据和重复组。
- `sync-manifest.json` / `sync-status.json`：受控镜像清单、最近同步计划与安全状态；绝对路径只存在本机 `.local.json`。
- 知乎下载登录态只保存在 Git 忽略的 `external-knowledge/zhihu-cookie.local`，由知乎入库下载器本机读取；不得进入来源、索引、日志、构建产物或提交。
- 知乎 Markdown 统一按作者名置于 `sources/zhihu-novels/<作者>/`；无法识别作者时使用 `未署名/`。目录名不得包含导入日期，归档日期只写在正文元数据的 `archivedOn`（并保留精确 `ingestedAt`）字段中，避免同一作者形成日期碎片目录。
- `index/`：按 500 段分片的定位索引、关键词、标签与来源反向定位。
- `cards/`：知识卡（当前以 `restraint/` 两级卡为主；`card-rules.json` 可再生成 term/plot-pattern 候选）；`reports/`：质量、重复、缺失元数据与直接照搬风险。
- `schemas/`：来源、分段和卡片契约；`scripts/external-knowledge/`：本地确定性工具。

数据链路为：原始来源 → 来源目录 → 文本分段 → 关键词/标签索引 → 抽象知识卡 → 结合项目正式知识原创转化。来源路径、sourceId、segmentId 与行号贯穿全链路。

## 知识卡

当前正式在用的是带 `ragLayer` / `parentCardIds` 的两级卡（上位类别 → 具体概念），主要落在 `cards/restraint/`。Schema 仍允许历史类型名（`expression`、`visual-structure`、`scene-pattern`、`fictional-state`、`trope`、`term`、`plot-pattern` 以及 tool/effect 等），但**不再自动播种**空泛的表达/场景/套路占位卡。

`term` 与 `plot-pattern` 仅在 `card-rules.json` 写明证据组且命中足够独立来源时才生成候选；证据不足时不产出。构建索引时会保留手维护卡（如 restraint 骨架/已补卡），不会因规则为空而整目录清空。

自动卡只标 `candidate`；经明确抽象审核后才标 `reviewed`/`confirmed`。卡片正文不得复制来源句子（`directQuoteIncluded: false`）；原文证据集中存放在共享证据库，由 `evidenceRefs` / `claims[].evidenceRefs` 引用，运行时按源文件行号展开。

知识结构为三层：

```text
原始来源（catalog/sources + Style-RAG article-registry）
  ↓
原文证据片段（external-knowledge/evidence/）
  ↓
RAG 知识卡 claims / definition（整理后的可核验陈述）
```

骨架卡（`contentStatus: stub`）可出现在图谱并作为关系锚点，但 `retrievalPolicy.contentRetrievable` 默认为 false，不得注入正式写作上下文。AI 候选证据默认 `pending`，用户核验后才成为正式证据。

撰写或改写知识卡正文、抽查协商与改稿说明前，先应用 `liluo-natural-expression` **light**：说清这张卡干什么、和邻居差在哪，避免口号空话。字段名、ID、路径与命令不润色。权威路由见 [璃落自然表达与文气塑形系统](./璃落自然表达与文气塑形系统.md)；人工抽查闭环见 [知识检索抽查校准系统](./知识检索抽查校准系统.md)；图谱证据详情见 [大纲关联图谱系统](./大纲关联图谱系统.md)。

## 查询与维护

```powershell
npm run external:knowledge:query -- --query "古堡 逃脱" --mode and --limit 8 --format markdown
npm run external:knowledge:query -- --query "挠痒"
npm run external:knowledge:query -- --query "手铐"
npm run external:knowledge:check
npm run external:knowledge:sync
npm run external:knowledge:update
npm run external:knowledge:build
npm run external:knowledge:validate
```

增量更新先比较权威源与镜像的路径和 SHA-256，识别新增、修改、删除与同哈希重命名，只复制变化内容；普通删除自动传播到清单管理的镜像文件，一次删除超过 20% 时自动阻止，防止盘符或目录异常被误判。随后只重解析变化来源，并如实重建全局关键词/标签索引。全量构建先写临时产物，再逐文件安全发布。重复来源只报告，不删除。

## 项目使用与原创性

唯一底线是：不直接照搬原文表达。先检索多个参考、抽象共同机制、去除专名和独特事件顺序，再读取项目正式索引与权威文件，围绕璃落世界、角色、地图、玩法和事件重新设计。外部知识不能反向覆盖项目事实。

日常使用保持两层：外部知识库是 Codex 使用的后台资料室，正式情节库是用户查看和采用的原创项目内容。术语卡、情节模式卡、来源 ID 与转换过程默认不进入情节展示页；它们只在检索、审查和原创性核验时使用。正式情节只展示结合璃落角色、世界、冲突与后果重新组织后的结果。

用户说“参考外部知识库升级情节”时，Codex 自动完成新鲜度检查、后台检索、抽象重组和照搬风险检查。情节目录不要求用户维护知识卡引用，也不因为知识库已经存在就把旧情节自动标成原创；只有实际完成重组与验证的内容才写入正式情节字段。

正式写入前运行：

```powershell
npm run external:knowledge:copy-check -- --input "待检查文件.md"
```

检查连续相同字符、长短语与 n-gram 重叠，返回来源路径/行号和 `low`、`medium`、`high`。报告不复制长原文，也不构成法律版权判断；中高风险应改写后复检。

## 状态、恢复与 Git

`current` 可直接使用；`stale` 先增量更新；`partial` 表示部分格式/来源未索引；`error` 先修生成器或数据契约；`not-indexed` 需首次构建。索引损坏、schema/分段变化或大量移动时运行安全全量重建，不手改生成文件。

建议提交通用配置、同步清单、schema、工具、卡片、目录/索引、报告与状态，以便可复现查询；含本机绝对路径的 `source-sync.local.json` 必须忽略。原始语料是否进入 Git 由仓库内容策略决定。不得提交 `.build-*`、`.publishing`、`.syncing`、备份和其他临时发布文件。

未来可按真实需求增加 SQLite FTS、本地全文增强、本地语义 Embedding、混合 RAG、MCP 查询、卡片候选生成和可视化浏览器；当前不接在线向量库、外部模型 API、云 RAG 或常驻服务。
