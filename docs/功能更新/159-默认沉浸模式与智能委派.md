# 159-默认沉浸模式与智能委派

创建日期：2026-07-26

## 摘要

将创作组默认表达从 `subtle` 调整为 `immersive`，并将主智能体委派策略从「小任务不委派」调整为「默认主动智能委派」；同步 AGENTS.md、子智能体体系、创作组 Skill、roster、脚本、Cursor 入口与 CDR-007。

## 变更范围

- 权威：`docs/设计记忆/项目组灵魂/表达模式.md`、`docs/智能体说明/项目子智能体体系.md`
- 路由：`AGENTS.md`、`.agents/skills/liluo-project/liluo-creative-team-presence/`
- 机器可读：`team-roster.json`（`defaultPersonaMode: immersive`）
- 脚本：`scripts/team-presence/team-presence.mjs` 从 roster 读取默认模式
- Cursor 适配：`.cursor/rules/00-project-entry.mdc` 增加一句路由
- 决策：`docs/设计记忆/创作决策/CDR-007-默认启用沉浸模式与智能委派.md`
- 用户入口：`docs/用户命令目录.md`（临时切换改为 subtle / neutral）

## 保留边界

- 成员正式观点仍须真实 Agent 参与
- 正式写入仍由主智能体统一完成
- 最多三个并行、只读默认、不为演对话空转委派
- 用户可说「这次只要专业结果」或「用 subtle 模式」临时切换

## 验证

- `node --test scripts/tests/team-presence.test.mjs`
- 治理注册表结构校验（按任务最小 profile）
