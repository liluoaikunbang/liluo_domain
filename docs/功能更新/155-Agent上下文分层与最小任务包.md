# 155-Agent 上下文分层与最小任务包

- 更新日期：2026-07-25
- 当前摘要：将项目通用边界收敛到根规则，把角色细节保留在角色说明与按需 Skill，并用最小任务包和短报告契约限制 Agent 会话载荷。

## 已实现

`AGENTS.md` 明确小任务不做宽索引、无关 Skill 或无必要委派；重型子问题只以目标、范围、已知事实、问题、约束和交付格式委派。子 Agent 回传结论、证据路径、风险、未决项和建议的短摘要，不回传整份索引、长原文或工具日志。

七个 Agent TOML 移除了重复的项目规则、人格来源和长流程清单，仅保留角色、权限、最小读取顺序与报告契约。角色的完整职责继续由 `docs/智能体说明/` 和命中的 Skill 维护，主 Codex 仍统一写入。

## 验证边界

本次只改变项目工作流与文档/Agent 配置，不改变游戏代码或运行时行为。验证应覆盖 Agent 配置发现、职责边界与项目索引；不需要 Web 构建。

## 主要路径

- `AGENTS.md`
- `.codex/agents/`
- `docs/智能体说明/项目子智能体体系.md`
