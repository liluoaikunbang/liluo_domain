# 174-移除大纲层级核对工作流

## 创建时间

2026-07-28

## 摘要

移除大纲中的“层级核对”页签、审计队列、`plot-layer:*` 命令和迁移脚本。既有情节、故事与 RAG 的已确认事实继续保留；后续跨层级调整改由对话中逐项确认，再按最小范围写入权威主数据。

## 实现要点

- 删除大纲层级核对面板及其队列数据。
- 删除 `scripts/plot-layer/`、对应测试和包脚本命令。
- 删除已废弃审核产物目录；权威系统说明与用户命令目录不再暴露该工作流。

## 验证

- `node --test scripts/tests/outline-plot-section.test.mjs`
- `npm run build:web`
- `npm run docs:commands:validate`
