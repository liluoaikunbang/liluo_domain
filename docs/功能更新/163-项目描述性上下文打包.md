# 163-项目描述性上下文打包

创建日期：2026-07-26

## 摘要

新增可增量维护的描述性上下文打包：把配置、文档、索引与故事/情节/玩法等描述数据复制到 gitignore 临时目录并压缩，供上传给其他 AI 分析；每次运行自动增删清单路径，避免全仓扫描。

## 变更范围

- Skill `liluo-project-context-pack`
- `scripts/project-context-pack/` 清单与打包脚本、单测
- npm：`project:context-pack` / `dry-run` / `test`
- `.gitignore` 忽略 `.local/project-context-pack/`
- 系统说明、技能说明、用户命令与治理注册

## 验证

- `npm run project:context-pack:test`
- `npm run project:context-pack`

## 后续补充（同日）

- 建立 `LINK-STATE` 正式↔临时关联：打包只处理删除、重复与可能新增的清单条目，并按 mtime/size 增量同步；输出固定为 `liluo-project-context-latest.zip`，不做全量重扫或整盘重拷。
- 默认删除同目录旧版 zip，只保留 latest；用户明确要求时可用 `--keep-previous`。
- 清理 staging 孤儿文件；排除 `external-knowledge/sources` 与 `docs/功能更新`；Skill 改为只跑命令读短 JSON，避免 Agent 读大文件耗 token。
