# 164-项目上下文分层与创作组有效参与

创建日期：2026-07-26

## 摘要

建立唯一机器可读的五层上下文装载策略，并分离创作组的人格模式、可见密度与真实参与规则；以三档路由提高日常有效微咨询，同时保持机械任务 solo、跨域任务最多三人。

## 变更范围

- `project-navigation/context-policy.json`、`team-routing.json`
- `schemas/workflows/context-policy.schema.json`、`team-routing.schema.json`
- `scripts/project-navigation/lib/context-routing.mjs` 与 `project:context:resolve` / `audit`
- roster schemaVersion 3（`presencePolicy`）
- 创作组 Skill/系统说明/子智能体说明、Cursor Always 薄适配
- ADR-012、治理注册表、用户命令与测试

## 验证

- `npm run project:context:test`
- `npm run project:context:audit`
- `npm run team:roster:validate`
- `npm run project:navigation:check`

## 更新时间

### 2026-07-26（同日后续）

- 收口历史功能编号：`017` 双文件改为 `017-a`/`017-b`，补齐 `118` 的目录与 `updateRecords`，并收紧编号审计正则。
- 为 `liluo-executable-workflow`、`liluo-project-context-pack` 补齐 eval 注册与 openai.yaml。
- 明确文档同步不是自动 Hook：强化 `AGENTS.md`、Cursor Always/`documentation-governance` 与 documentation-sync Skill 的完成前门禁。
