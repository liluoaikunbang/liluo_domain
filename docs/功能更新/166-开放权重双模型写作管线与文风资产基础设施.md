# 166-开放权重双模型写作管线与文风资产基础设施

- 创建时间：2026-07-26
- 更新时间：2026-07-26

## 摘要

建立 Zhi-Create-DSR1-14B / Qwen3-32B 的 OpenAI-compatible 写作管线、非正式工作区、写作合同、文风资产目录与缺口登记；StyleRAG 暂缓；live 前须 Running，默认再等约 5 分钟后双模型 health；登记成人向低拒答待添加候选（未接入生产）。

## 实现要点

- `project-navigation/writing-models.json` + Schema + CLI（validate/status/health/draft/compare/assets/calibration/pin）
- `docs/写作资产/**` 分层目录与空 registry；确认不扫描既有正文
- Skill `liluo-formal-prose-pipeline`；缺口写入 `manual-gaps.json`
- mock 测试默认不联网；`.env.writing.local` gitignore
- 规范补充：live 前用户须确认 Inference Endpoints 为 Running；控制台 Running 后默认再等约 5 分钟，再对双模型（或指定单模型）跑 live health；503 优先按休眠/冷启动处理，智能体不代启付费云端点
- 待添加候选：`docs/写作资产/模型归档/pending-candidates.json` 登记 `llmfan46/Qwen3.5-27B-Writer-V2-uncensored-heretic`（成人向低拒答）；缺口 `gap-writing-model-adult-uncensored-candidate`；不静默替换生产双模型

## 验证

见同次任务验证命令清单。2026-07-26 追加：Live 预检、Running 后约 5 分钟再 health、成人向 pending candidate 登记；`npm run docs:governance:audit`。
