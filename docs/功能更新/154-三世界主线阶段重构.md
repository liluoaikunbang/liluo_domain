# 154-三世界主线阶段重构

创建日期：2026-07-25

## 实现

重构寂土挽歌、咒缚回响与星宇织梦的故事树：以主线阶段容器替代把所有地区卷串成固定父子链的做法。寂土区分正史、异常灾域、末日轮回与后日谈；咒缚以诅咒进展组织阶段并保留地区卷池；科幻以航程弱主线组织独立星球、边境与深空篇章。

新增旧节点原文保全与迁移去向清单，以及确定性校验。校验保护旧节点的非结构化源字段、已有 Markdown 与 `missingItems`，允许本次变更 `parentKey` 和 `order`。

## 边界

阶段容器只记录已确认的结构职责。未被旧原文或用户本次指示确认的因果、触发、规则、长期状态与结局保留为项目导航缺口；不因重构删除、合并或润色旧灵感。

## 验证

运行原文保全、故事图结构、索引、数据合同与导航的最小范围检查；不运行 Web 构建或浏览器回归。
